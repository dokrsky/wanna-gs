# UI-08 컨텍스트 계약 · revision 1

2026-09-21 조정자 작성. D-46 화면 우선 순서를 따른 통합 인계 계약이다. 이전 메시지의 소유권/DTO 합의를 파일로 연결하며 과거에 manifest hash ACK가 있었다고 소급하지 않는다. 이 파일의 SHA256은 PROGRESS와 후속 ACK에 기록한다(자기 참조 hash 제외).

## 목적·기준

- 고객이 요청의 현재 대기 이유와 실제 픽업 가능/마감을 읽을 수 있어야 한다. 정상 자연어→동의→요청→공급/모의 결제→입고/48시간→수령을 유지한다.
- branch `codex/ui-preview-20260921`, base `81a4369ec2037886992fbb6869389a27c6df6810`, 대상은 로컬 및 보호된 Vercel Preview다. Production은 변경하지 않는다.
- CORE-04/07~10/24~26, AC-04/11~17/30~32. D-44 SQLite·D-45 서버 모델·D-46 구현 우선. ADR-003/004/005 adopted의 기존 범위이며 정책 변경은 없다.
- `card.md` SHA256 `62fae686c1967902ac443a0a315f4c7f59f76df9e6de3a3568e4355d6e334e03`; CORE `65c7541d3549bf49b715da687aa5af060fce9a48e774a4130d9ac350349d8114`; 원장 `aa75d143ddefdd500adadd2fb6436a26683b1609af274b64680b36d5a0c0de15`.
- ADR-003 `66c252563cd54aae7c397f553d53fe7216f8e01e270bcdb90ec4329d82f28353`; ADR-004 `f04c34ef1831f62600c24a737345093271234ffc07b48e1dd3f292c1f161ce7a`; ADR-005 `518cbfa6dfa179a8b377428ae3c7451c0e54933c909066070487cc941be264a0`. 대체·충돌 없음.

## 소유·계약

- Locke `01a0c40b-6761-7302-877f-f9a9bc997484`: `lib/domain/{types.ts,commands.ts,check.mjs,wait.check.mjs}`. 공통 DTO 단일 작성, handback 완료.
- Newton `01a0c40d-fd69-7871-a336-8f84e14787ed`: `app/components/customer-workspace.{tsx,module.css}`, `domain-workspace.{tsx,module.css}`.
- 조정자: 문서·후보 통합·빌드·커밋/푸시·Preview 확인. Meitner `01a0c40b-66e4-76e3-93a0-ac10d87259c9`: 위 diff만 읽기 전용 좁은 통합 검토. 전체 제품 게이트나 독립 역할 UX 판정이 아니다.
- `CustomerWaiting`: code 8개, checkedAt, 최소·포장 대기일 때만 공개 모의 moq/packSize. 취소/예약 이후에는 null. 조회만으로 정산/발주/결제/시계/receipt를 변경하지 않는다.
- `RequestDetailLine.supplied:boolean`; 고객 조회에서 매입가·전체 발주/공급 수량을 생략하고 경영주 조회는 유지한다. 기존 SQL/정책/명령/seed 변경 없음.
- schema2 sourceHash `3d15fce9caf74e0293ce5908bb532cbd3dc69e4639df573b5f76553f1fc72bc0`, seed SHA256 `b8058996893c45b4df9f8e5cd32ece2973ad7a7a3d5dc9399556d01d8a7d8864`. 기존 모델/프롬프트/평가셋은 변경하지 않는다.
- 선행 UI-07 Ready, 후행 UI-09 경영주 문맥·실행 이력과 전체 검증. 이 작업 종료는 작은 Preview 게시이며 goal 종료가 아니다.

## 근거·도구·확인 범위

- D39 최종 이미지 `docs/assets/wanna-gs-promo-mascot.png` SHA256 `ff1f572a8030fa5bdce6b98ab9d91d796dfbae80a62d4a229209f109a944b876`를 조정자/Newton이 직접 열람했다. 26번의 발음 라벨·확인 순서·픽업 위계만 반영한다. 실측 고객조사라고 주장하지 않는다.
- 신규 GS 사실/외부 기술 채택 없음: 외부 재조사는 not_applicable. waiting 검사 입력은 synthetic이며 실제 재고·판매 예측이 아니다. 기존 정상 거래와 read-only/민감정보 반례에 연결한다.
- 실제 도구 catalog에서 multi-agent 도구 확인, 기존 3개 agent 사용. 별도 모델 override 없음(상속 모델의 구체 이름은 미기록). 개발 agent와 앱 runtime 모델은 별개다.
- runtime `gpt-5-mini`/OpenAI 직접 호출, live는 기존 Preview 증거를 유지하되 UI-08 추가 live 호출은 계획하지 않는다. 계정 잔액/전체 quota는 unknown, 새 결제/한도 상향 없음.
- Node24, 실제 sql.js, CUA 브라우저, gh/Vercel CLI는 이전 단계에서 사용 확인. `.env.local` 정적 재확인은 CONFIGURED/live not_run, 키 값 비출력/Git 제외 확인.
- 자체 domain/대기 투영 검사·정확한 후보 빌드·변경 화면 확인만 우선 실행한다. SQL/브라우저 장애·전체 품질·독립 UX·G5/G6는 후속이며 미실행을 PASS로 올리지 않는다.

DTO 소비자 메시지 ACK는 완료했고 이 파일 hash의 사후 인계 ACK/차이는 PROGRESS에 기록한다. 다음 정책·스키마·행동 변경 시 새 계약을 만들고 영향 결과만 stale 처리한다.
